
doctype html
html
  head
    title Istoria Comunei Hiliseu Horia
    link(rel='stylesheet' href='/css/style.css')
  body
    a(href='http://127.0.0.1:8080/html1.html') Incepe 
    nav.navbar.navbar-inverse
      .container
        .navbar-header
          span.icon-bar
          span.icon-bar
          span.icon-bar
          a.navbar-brand(href='/') Istoria Comunei Hiliseu Horia
        #navbar.collapse.navbar-collapse
          ul.nav.navbar-nav 
            li
              a(href='/') Home
            li
              a(href='/articles/add') Add Article
          ul.nav.navbar-nav.navbar-right
    .container
      != messages('message', locals)
      if errors
        each error, i in errors
          div(class="alert alert-danger") #{error.msg}
      block content
      br
      hr
      footer
    script(src='/js/main.js') 